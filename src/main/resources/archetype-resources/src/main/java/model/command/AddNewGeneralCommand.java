package ${package}.model.command;

import com.core.springboot.command.Command;
import ${package}.model.schema.GeneralSchema;

public class AddNewGeneralCommand extends GeneralSchema implements Command {
}
